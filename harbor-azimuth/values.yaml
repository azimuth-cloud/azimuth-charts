zenithClient:
  iconUrl: https://raw.githubusercontent.com/goharbor/harbor/refs/heads/main/docs/img/harbor_logo.png
  description:
  label: "Harbor Registry"

harbor:
  expose:
    type: clusterIP
    tls:
      enabled: false
    clusterIP:
      name: harbor

  externalUrl: http://harbor:443 # must match clusterIP service name
