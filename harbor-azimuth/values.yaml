zenithClient:
  iconUrl:
  description:
  label: "Harbor Registry"

harbor:
  expose:
    type: clusterIP
    tls:
      enabled: false
    clusterIP:
      name: harbor

  externalUrl: http://harbor:443 # must match clusterIP service name
