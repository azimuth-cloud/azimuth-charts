{
    "$schema": "http://json-schema.org/schema#",
    "type": "object",
    "properties": {
        "nodes": {
            "type": "integer",
            "title": "Compute nodes",
            "description": "The number of nodes to spawn slurmd pods on",
            "minimum": 1,
            "default": 1
        }
    },
    "required": ["nodes"]
}