apiVersion: zenith.stackhpc.com/v1alpha1
kind: Client
metadata:
  name: {{ include "aiwb-azimuth.fullname" . }}
  labels: {{ include "aiwb-azimuth.labels" . | nindent 4 }}
spec:
  reservationName: {{ include "aiwb-azimuth.fullname" . }}
  upstream:
    serviceName: argocd-status-service
